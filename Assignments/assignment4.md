# Assignment4: Handling Tooltips

## Instructions

1. **Write test cases** to interact with tooltips on the page:
   - Visit: `http://localhost:4200/pages/modal-overlays/tooltip`
   - Locate tooltip elements and trigger their display (e.g., hover or focus).
   - Add **assertions** to verify tooltip text and visibility.

2. **Locator Strategy**
   - Use Playwright locators to identify tooltip triggers and tooltip content.
   - Reuse locator variables for maintainability.

3. **Debugging Hint**
   - Pause the debugger in your browser to inspect and locate tooltip elements. (F8 or ctrl+/)

## Example Branch Workflow

```bash
git checkout -b <your-branch-name>
git pull origin main
```

4. **Push your branch to GitHub:**

```bash
git push -u origin <your-branch-name>
```